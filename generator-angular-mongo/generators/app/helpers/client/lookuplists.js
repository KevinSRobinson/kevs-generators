const copier = require('../templateCopier.js');

module.exports.generate = function (data, runner, _srcPath, destPath) {

  // var _srcNavigationComponentsPath = _srcPath;

  // var destNavigationComponentsPath = destPath + '/Components/';


    this.directory('scripts', 'scripts')

    // runner.fs.copyTpl(
    // runner.templatePath('MVP/Details/UserControl/_uCDetails.Designer.vb'),
    // runner.destinationPath(featureFolderName + '/Details/' + userControlName + '.Designer.vb'), {
    //   data: data
    // }
  );
};
